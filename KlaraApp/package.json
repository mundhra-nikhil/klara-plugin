{
  "name": "klara-app-workspace",
  "version": "1.0.0",
  "scripts": {
    "start:backend": "cd backend && docker-compose -f docker/docker-compose.yml up",
    "dev:all": "concurrently \"npm run start:backend > .backend.log 2>&1\" \"cd frontend && npm run dev-server > ../.frontend.log 2>&1\" \"cd frontend && npm start > ../.word.log 2>&1\"",
    "start": "cd frontend && npm start",
    "stop": "cd frontend && npm run stop",
    "build:dev": "cd frontend && npm run build:dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
